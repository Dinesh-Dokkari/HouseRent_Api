@model IEnumerable<HappyHouse.Models.HouseNumberDto>

<br />
<div class="container p-4 border">
	<div class="row pb-2">
		<div class="col">
			<h1 class="text-primary">House Number List</h1>
		</div>
		<div class="col text-end pt-1">
			<a asp-controller="HouseNumber" asp-action="CreateHouseNumber" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i> Create New HouseNumber</a>
		</div>
	</div>

	<table class="table">
		<thead>
			<tr>
				<th>
					House Number
				</th>
				<th>
					House Name
				</th>
				<th>
					Special Details
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>@item.HouseNo</td>
					<td>@item.House.Name</td>
					<td>@item.SpecialDetails</td>
					<td>
						<a asp-controller="HouseNumber" asp-action="UpdateHouseNumber" asp-route-houseid="@item.HouseNo" class="btn btn-success"><i class="bi bi-pencil-square"></i></a>
						<a asp-controller="HouseNumber" asp-action="DeleteHouseNumber" asp-route-houseid="@item.HouseNo" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>
					</td>
				</tr>
			}
		</tbody>
	</table>

</div>
